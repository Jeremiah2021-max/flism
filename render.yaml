services:
  # ─────────────────────────────────────────────────────────────
  # Student App + API  →  https://flism-app.onrender.com
  # ─────────────────────────────────────────────────────────────
  - type: web
    name: flism-app
    runtime: node
    env: node
    region: frankfurt
    plan: free
    buildCommand: >-
      cd server && npm install --no-workspaces --legacy-peer-deps && cd ../mobile &&
      npm install --no-workspaces --include=dev --legacy-peer-deps && EXPO_PUBLIC_API_URL=https://flism-app.onrender.com ./node_modules/.bin/expo export --platform web --output-dir ../server/public
    startCommand: NODE_ENV=production node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: PAYSTACK_SECRET_KEY
        sync: false
      - key: PAYSTACK_PUBLIC_KEY
        sync: false
      - key: RENDER_EXTERNAL_URL
        value: https://flism-app.onrender.com
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: ADMIN_NAME
        sync: false

  # ─────────────────────────────────────────────────────────────
  # Admin Panel  →  https://flism-admin.onrender.com
  # ─────────────────────────────────────────────────────────────
  - type: web
    name: flism-admin
    runtime: static
    buildCommand: >-
      cd admin-app && npm install --no-workspaces --include=dev --legacy-peer-deps &&
      NODE_ENV=production EXPO_PUBLIC_API_URL=https://flism-app.onrender.com ./node_modules/.bin/expo export --platform web --output-dir dist
    staticPublishPath: admin-app/dist
    envVars:
      - key: EXPO_PUBLIC_API_URL
        value: https://flism-app.onrender.com
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
